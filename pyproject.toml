[project]
name = "skill-manager"
version = "0.1.0"
description = "跨设备 Skill 管理软件"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.5.0",
    "paramiko>=3.0.0",
    "cryptography>=41.0.0",
    "keyring>=24.0.0",
]

[project.scripts]
skill-manager = "src.main:main"
