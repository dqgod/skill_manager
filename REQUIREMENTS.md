# 跨设备 Skill 管理软件 — 需求规格说明书

## 1. 项目概述

### 1.1 项目背景

当前 AI 编码助手（Codex、Claude Code 等）通过 skill 机制扩展能力，但 skill 文件分散在不同工具、不同设备的多个目录中，且存在两个层级：

- **全局 Skill**：存放于用户目录下，所有项目共享
- **项目 Skill**：存放于项目目录下的 `.claude/skills`、`.codex/skills` 中，仅对该项目生效

用户在多台机器、多个项目间手动维护 skill 的一致性成本高且易出错。

### 1.2 项目目标

构建一个跨设备 skill 管理工具，运行于 Windows 平台，能够：

- 读取本机（Windows）及远程机器（Linux）上各工具的 skill
- 管理项目级别的 skill（注册项目目录，独立管理）
- 在全局 skill 与项目 skill 之间同步
- 对比、去重 skill
- 按需在设备间同步 skill，并选择同步到指定的工具目录

### 1.3 适用范围

| 维度       | 说明                        |
| ---------- | --------------------------- |
| 运行平台   | Windows 10+                 |
| 管理对象   | 本机 Windows + 远程 Linux   |
| 支持工具   | Codex、Claude Code、CC-Switch |
| Skill 层级 | 全局级、项目级              |
| 用户群体   | 使用多设备、多 AI 编码工具的开发者 |

---

## 2. 功能需求

### 2.1 Skill 存放路径规范

#### 2.1.1 全局 Skill — Windows 端

| 工具         | 路径                                |
| ------------ | ----------------------------------- |
| Codex        | `%USERPROFILE%/.codex/skills`       |
| Claude Code  | `%USERPROFILE%/.claude/skills`      |
| CC-Switch    | `%USERPROFILE%/.cc-switch/skills`   |

#### 2.1.2 全局 Skill — Linux 端

| 工具         | 路径                        |
| ------------ | --------------------------- |
| Codex        | `~/.codex/skills`           |
| Claude Code  | `~/.claude/skills`          |
| CC-Switch    | `~/.cc-switch/skills`       |

#### 2.1.3 项目级 Skill — Windows / Linux 通用

| 工具         | 路径                        |
| ------------ | --------------------------- |
| Codex        | `<项目根>/.codex/skills`    |
| Claude Code  | `<项目根>/.claude/skills`   |

> 项目级 skill 仅在该项目目录下生效，优先级高于同名全局 skill。

---

### 2.2 核心功能

#### F1 — 读取本机 Skill（全局 + 项目）

**描述：** 扫描 Windows 本机的全局 skill 目录以及已注册项目的 skill 目录，列出所有已安装的 skill。

**输入：** 选择工具范围（Codex / Claude Code / CC-Switch，可多选或全选）。

**输出：** 每个 skill 的名称、来源工具、路径、所属层级（全局/项目）、文件大小、最后修改时间。

**规则：**
- 若某工具的 skill 目录不存在，视为该工具下无 skill。
- 以 skill 文件/子目录名称作为 skill 标识。
- 未注册项目时仅显示全局 skill。

#### F2 — 读取远程机器 Skill

**描述：** 通过 SSH 连接远程 Linux 机器，扫描其 skill 目录（全局 + 已注册项目）。

**输入：**
- 远程机器地址（IP 或主机名）
- SSH 端口（默认 22）
- 认证方式（密钥 / 密码）
- 选择工具范围（Codex / Claude Code / CC-Switch）

**输出：** 同 F1，额外标注所属设备和层级。

**规则：**
- 连接失败应给出明确错误提示（超时、认证失败、主机不可达）。
- 需缓存连接配置，支持复用。

#### F3 — Skill 去重与对比

**描述：** 将已读取的 skill 进行汇总、去重和差异对比。

**去重规则：**
- 以 skill 名称作为唯一标识。
- 同一 skill 出现在多台设备/多个工具目录/多个层级时，视为同一 skill 的多个副本。
- 按设备、层级维度展示每个 skill 的分布情况。

**差异对比维度：**
- 仅在 A 设备存在的 skill
- 仅在 B 设备存在的 skill
- 两设备均存在但内容不同的 skill（基于文件哈希或修改时间）
- 仅在全局存在的 skill vs 仅在项目存在的 skill

**输出：** 分类展示的 skill 对比视图。

#### F4 — Skill 同步

**描述：** 将选中的 skill 从源同步到目标。

**同步方向：**
- 本机 → 远程机器（推送）
- 远程机器 → 本机（拉取）

**同步范围（层级维度）：**
- 全局 → 项目：将全局 skill 复制到指定项目的 skill 目录中
- 项目 → 全局：将项目 skill 提升为全局 skill
- 本机全局 ↔ 远程全局
- 本机项目 ↔ 远程项目（同名项目）
- 项目 A → 项目 B（同设备或跨设备）

**目标工具选择：** 同步时可指定目标为 Codex、Claude Code、CC-Switch 中的一项或多项。

**同步行为：**
- 若目标已存在同名 skill，提示用户选择覆盖或跳过。
- 支持批量选择 skill 进行同步。
- 同步过程显示进度（传输中 / 完成 / 失败）。
- 同步完成后刷新对应视图。

**冲突处理：**
- 默认策略：交互式确认（覆盖 / 保留双方 / 跳过）。
- 提供"全部覆盖"、"全部跳过"快捷选项。

#### F5 — SSH 连接管理

**描述：** 管理远程 Linux 机器的连接配置。

**功能：**
- 新增连接配置（主机、端口、用户名、认证方式）。
- 编辑 / 删除已有配置。
- 测试连接可用性。
- 连接配置持久化存储（本地加密保存密码，密钥仅存路径）。

#### F6 — 项目管理

**描述：** 注册并管理本地及远程项目目录，实现项目级 skill 的独立管理。

**功能：**
- **注册本机项目：** 选择本地目录，将其注册为"项目"，自动识别该项目下的 `.claude/skills`、`.codex/skills` 目录。
- **注册远程项目：** 关联远程 Linux 机器上的项目路径，同样识别其项目 skill 目录。
- **编辑项目信息：** 修改项目名称、路径、关联的远程路径等。
- **删除项目：** 从管理列表中移除项目（不删除实际项目文件）。
- **项目列表：** 展示所有已注册项目及其 skill 概况。

**项目属性：**

| 属性            | 说明                                   |
| --------------- | -------------------------------------- |
| 项目名称        | 用户自定义的显示名称（如"开发任务A"）   |
| 本机路径        | 项目在本机的绝对路径                    |
| 关联远程设备    | 可选，关联的远程 Linux 连接配置          |
| 远程路径        | 可选，项目在远程机器上的绝对路径         |
| 支持工具        | 该项目使用哪些工具（Codex / Claude Code）|

**规则：**
- 同一目录不可重复注册。
- 删除项目时，可选择是否同时清理该项目下的 skill 目录。
- 项目 skill 目录若不存在，可在用户确认后自动创建。

---

### 2.3 用户界面功能

#### UI1 — 仪表盘

- 概览：全局 skill 总数、各项目 skill 数、远程 skill 总数、待同步数量、差异数量。
- 快捷操作入口：读取全部、一键同步。
- 最近项目入口。

#### UI2 — Skill 浏览面板

- 按"全局"和各"项目"分 Tab 或分组展示。
- 树形或列表展示，按设备/工具/层级分组。
- 支持搜索、过滤（按工具、按设备、按层级、按同步状态）。
- 支持多选。

#### UI3 — 项目管理面板

- 已注册项目列表，每项显示项目名称、路径、skill 数量。
- 切换项目视图，独立管理每个项目的 skill。
- 拖拽 skill 到不同层级/项目实现快速同步。

#### UI4 — 同步操作面板

- 选择同步方向（推送 / 拉取）。
- 选择同步层级（全局 ↔ 全局 / 全局 → 项目 / 项目 → 全局 / 项目 → 项目）。
- 选择源项目（项目级同步时）。
- 选择目标工具（多选复选框）。
- 选择目标远程机器（跨设备同步时）。
- 预览待同步 skill 列表。
- 执行同步并显示进度。

#### UI5 — 日志与历史

- 记录同步操作历史（时间、方向、源/目标层级、涉及 skill、结果）。
- 支持按项目、按设备筛选历史。
- 支持查看详细日志。

---

## 3. 非功能需求

### 3.1 性能

| 指标              | 要求                    |
| ----------------- | ----------------------- |
| 本机扫描          | 3 秒内完成              |
| 远程扫描          | 10 秒内完成（局域网）    |
| 单个 skill 同步   | 5 秒内完成（常规大小）   |

### 3.2 安全性

- SSH 密码加密存储（AES-256）。
- 不在日志中记录明文密码。
- 远程传输使用 SSH 加密通道。

### 3.3 可用性

- 界面语言：中文。
- 提供操作引导（首次使用时的新手提示）。
- 错误信息提供可操作的解决建议。

### 3.4 兼容性

- Windows 10 22H2+ / Windows 11。
- 远程 Linux 支持主流发行版（Ubuntu 20.04+、CentOS 7+、Debian 11+）。
- SSH 协议版本：SSH-2。

### 3.5 可靠性

- 同步中断时不影响目标端已有 skill 的完整性。
- 操作支持回滚（同步前备份，失败时恢复）。

---

## 4. 技术约束

| 项目         | 选型建议                            |
| ------------ | ----------------------------------- |
| 开发语言     | Python 3.10+ 或 Go 1.21+           |
| GUI 框架     | PySide6 / WPF / Electron            |
| SSH 库       | Paramiko (Python) / crypto/ssh (Go) |
| 数据存储     | SQLite（连接配置、项目配置、操作历史）|
| 配置加密     | 操作系统凭据管理器 / AES-256         |

---

## 5. 数据模型

### 5.1 连接配置

| 字段         | 类型     | 说明                  |
| ------------ | -------- | --------------------- |
| id           | UUID     | 主键                  |
| name         | string   | 连接名称（用户自定义）|
| host         | string   | 主机名或 IP           |
| port         | int      | SSH 端口，默认 22     |
| username     | string   | SSH 用户名            |
| auth_type    | enum     | key / password        |
| key_path     | string   | 私钥路径（key 模式）  |
| password_enc | blob     | 加密密码（password 模式） |
| created_at   | datetime | 创建时间              |
| updated_at   | datetime | 最后修改时间          |

### 5.2 项目配置

| 字段            | 类型     | 说明                              |
| --------------- | -------- | --------------------------------- |
| id              | UUID     | 主键                              |
| name            | string   | 项目显示名称                      |
| local_path      | string   | 本机项目绝对路径                   |
| remote_device_id| UUID     | 关联的远程设备 ID（可空）          |
| remote_path     | string   | 远程项目绝对路径（可空）           |
| tools           | string   | 使用的工具列表，逗号分隔           |
| created_at      | datetime | 创建时间                          |
| updated_at      | datetime | 最后修改时间                      |

### 5.3 Skill 记录

| 字段         | 类型     | 说明                          |
| ------------ | -------- | ----------------------------- |
| id           | UUID     | 主键                          |
| name         | string   | skill 名称                    |
| device       | enum     | local / remote                |
| device_name  | string   | 设备标识                      |
| level        | enum     | global / project              |
| project_id   | UUID     | 所属项目 ID（全局级为 null）   |
| tool         | enum     | codex / claude / cc-switch    |
| path         | string   | 完整路径                      |
| hash         | string   | 文件内容 SHA-256              |
| size         | int      | 文件大小（字节）              |
| modified_at  | datetime | 最后修改时间                  |

### 5.4 同步历史

| 字段             | 类型     | 说明                              |
| ---------------- | -------- | --------------------------------- |
| id               | UUID     | 主键                              |
| timestamp        | datetime | 操作时间                          |
| direction        | enum     | push / pull                       |
| source_device    | string   | 源设备                            |
| source_level     | enum     | global / project                  |
| source_project_id| UUID     | 源项目 ID（可空）                  |
| source_tool      | enum     | 源工具                            |
| target_device    | string   | 目标设备                          |
| target_level     | enum     | global / project                  |
| target_project_id| UUID     | 目标项目 ID（可空）                |
| target_tool      | enum     | 目标工具                          |
| skill_name       | string   | 同步的 skill 名称                  |
| status           | enum     | success / failed / skipped        |
| detail           | string   | 备注（失败原因等）                 |

---

## 6. 用户故事

| 编号 | 角色     | 需求描述                                                              | 对应功能 |
| ---- | -------- | --------------------------------------------------------------------- | -------- |
| US1  | 开发者   | 我想查看本机 Windows 上所有全局 skill，了解当前的 skill 配置情况        | F1       |
| US2  | 开发者   | 我想连接远程 Linux 开发机，查看上面的 skill 配置                        | F2, F5   |
| US3  | 开发者   | 我想对比本机和远程机器的 skill 差异，快速发现哪些 skill 缺失或不一致     | F3       |
| US4  | 开发者   | 我想把本机 skill 推送到远程，或把远程 skill 拉取到本机                   | F4       |
| US5  | 开发者   | 同步时我想选择同步到 Codex、Claude Code 还是 CC-Switch，不必全部同步    | F4       |
| US6  | 开发者   | 我想把"开发任务A"注册为项目，单独管理这个项目专属的 skill               | F6       |
| US7  | 开发者   | 我想把本机全局 skill 复制到"开发任务A"项目的 `.claude/skills` 下        | F4       |
| US8  | 开发者   | 我想把"开发任务A"项目特有的 skill 同步到远程同一项目的对应目录           | F4       |
| US9  | 开发者   | 我想把项目中沉淀的 skill 提升为全局 skill，供其他项目复用               | F4       |
| US10 | 开发者   | 我想查看同步历史，了解哪些 skill 何时被同步到了哪里                    | UI5      |

---

## 7. 界面原型建议

### 7.1 主窗口布局

```
┌──────────────────────────────────────────────────────────────────┐
│  菜单栏: 连接管理 | 项目管理 | 操作历史 | 设置                      │
├───────────┬───────────────────────┬──────────────────────────────┤
│  Skill 视图│  本机 (Windows)        │  远程 (Linux)                │
│           │                       │                              │
│ ┌全局 ────│───────────────────────│──────────────────────────────│
│ │ Codex   │  skill-A, skill-B     │  skill-A, skill-C            │
│ │ Claude  │  skill-A, skill-D     │  skill-A, skill-E            │
│ │ CC-Switch│ skill-A              │  skill-F                     │
│ ├─────────│───────────────────────│──────────────────────────────│
│ │项目 ────│───────────────────────│──────────────────────────────│
│ │ 开发任务A│                      │                              │
│ │  .claude │ skill-X (项目专属)   │  skill-X (远程已同步)         │
│ │  .codex  │ skill-Y              │  skill-Y                     │
│ │ 网站重构 │                      │                              │
│ │  .claude │ skill-Z              │  -                           │
│ └─────────│───────────────────────│──────────────────────────────│
├───────────┴───────────────────────┴──────────────────────────────┤
│  同步方向: [推送选中 →]  [← 拉取选中]                              │
│  同步层级: [全局 ↔ 全局] [全局 → 项目] [项目 → 全局] [项目 ↔ 项目]  │
│  目标工具: [✓]Codex  [✓]Claude  [✓]CC-Switch                      │
│  状态栏: 就绪 | 项目: 2 | 上次同步: 2026-05-18 10:30              │
└──────────────────────────────────────────────────────────────────┘
```

### 7.2 添加项目对话框

```
┌────────────────────────────────────────────┐
│  注册项目                                   │
│  ┌────────────────────────────────────────┐ │
│  │ 项目名称:  [开发任务A_____________]      │ │
│  │ 本机路径:  [D:\projects\task-a____] [浏览]│ │
│  │ 关联远程:  [✓] 关联远程机器              │ │
│  │   远程设备: [dev-server-01 ▼]            │ │
│  │   远程路径: [/home/user/task-a___]       │ │
│  │ 支持工具:  [✓]Codex [✓]Claude Code       │ │
│  └────────────────────────────────────────┘ │
│                         [取消]  [注册]       │
└────────────────────────────────────────────┘
```

### 7.3 同步进度对话框

```
┌──────────────────────────────────────────────┐
│  正在同步...                                   │
│  方向: 本机全局 → 开发任务A (本地项目)           │
│  目标: .claude/skills                          │
│  ┌──────────────────────────────────────────┐ │
│  │ skill-A  → 项目/开发任务A/.claude  ✔ 完成 │ │
│  │ skill-D  → 项目/开发任务A/.claude  ⟳ 传输 │ │
│  │ skill-E  → 项目/开发任务A/.claude  ○ 等待 │ │
│  └──────────────────────────────────────────┘ │
│  进度: 1/3                                   │
│                              [取消]           │
└──────────────────────────────────────────────┘
```

---

## 8. 后续扩展（Out of Scope / V2）

- 支持 macOS 设备
- 定时自动同步
- Skill 版本管理与回滚
- Skill 在线市场集成
- 差异内容可视化（diff 视图）
- CLI 模式支持（用于脚本化 / CI 集成）
- 项目 skill 继承/引用（项目 A 引用项目 B 的 skill，避免重复拷贝）

---

## 9. 术语表

| 术语      | 说明                                                       |
| --------- | ---------------------------------------------------------- |
| Skill     | AI 编码助手的扩展能力单元，以文件或目录形式存放于指定路径     |
| 全局 Skill | 存放于用户目录下的 skill，所有项目共享                       |
| 项目 Skill | 存放于项目 `.claude/skills` 或 `.codex/skills` 下的 skill    |
| 本机      | 运行本管理软件的 Windows 机器                                |
| 远程机器  | 通过 SSH 连接的 Linux 开发机                                 |
| 推送      | 将 skill 从源复制到目标                                      |
| 拉取      | 从远程机器获取 skill 到本机                                   |
| CC-Switch | Claude Code 的切换/配置工具                                  |

---

## 10. 修订历史

| 版本 | 日期       | 修订内容                                         | 作者   |
| ---- | ---------- | ------------------------------------------------ | ------ |
| v1.0 | 2026-05-18 | 初稿，包含全局 skill 管理、项目级 skill 管理功能   | Claude |
